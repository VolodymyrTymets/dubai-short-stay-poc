{
  "name": "my-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:guest": "npm run dev -w packages/app-dashboard",
    "dev:admin": "npm run dev -w packages/app-admin",
    "build:all": "npm run build --workspaces"
  }
}